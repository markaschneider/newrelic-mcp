runtime: "typescript"
entry: "src/server.ts"
start:
  command: ["node", "dist/server.js"]
  port: 8000
env:
  NODE_ENV: "production"

# For HTTP transport (remote hosting), use:
# start:
#   command: ["node", "dist/http-server.js"]
#   port: 8000
